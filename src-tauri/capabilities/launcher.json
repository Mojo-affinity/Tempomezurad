{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "launcher",
  "description": "Capability for the launcher window",
  "windows": ["launcher"],
  "permissions": [
    "core:default"
  ]
}
